-------------------------------------------------------------------------------------------------------------------------------------
arrange
-------------------------------------------------------------------------------------------------------------------------------------
arrange directory contain a c file for arrange file with there format(pdf, jpg, png and mp4 are handled).

for compile - from shell change in to the directory arrange and run make file by just typing make and enter.
for use arrange program run ./arrange [argument1] [argument2]
argument1 :is  the directory for arrange files from this folder.
argument2 :is optional and if provide it will set as destination directory else it will be arrange in separate folders in argument1.




-------------------------------------------------------------------------------------------------------------------------------------
notify
-------------------------------------------------------------------------------------------------------------------------------------
notify directory contain a c file for monitor a directory for creation deletion and modification of files in that direcory.

for compile - from shell change in to the directory notify and run the command by just typing  cc inotify.c -o notify and enter.
for use notify program from shell just run ./notify [argument1]
argument1 : is the directory to monitor.
